# Зафиксированные зависимости прода.
#
# Их нет: pumpparse целиком на стандартной библиотеке Python (>=3.11).
# Файл не удалён специально — установка на сервере идёт через
#     pip install --no-deps --no-build-isolation .
# и любая новая строка здесь обязана появляться вместе с записью в
# pyproject.toml [project].dependencies и точным ==пином.
#
# Проверить, что дерево осталось чистым:
#     pip list --not-required --format=freeze
